# 刷题软件 · 网页版使用说明

本网页版是原命令行（终端版）的图形界面替代品，**共用同一份数据**：
`data/` 目录（题库 JSON、错题本 wrong_book.json、刷题进度 brush_progress.json）与 `reports/` 目录。

## 1. 如何启动

| 方式 | 命令 |
| --- | --- |
| 双击批处理 | 双击 `start.bat`，在菜单中选择「2. 网页版」 |
| 直接网页版 | 双击 `start_webui.bat` 或 `python webui_server.py` |
| 选择版本 | `python launcher.py`（1=终端版 2=网页版 0=退出） |
| 指定端口 | `python webui_server.py --port 9000` 或 `python launcher.py web --port 9000` |

启动后浏览器会自动打开 `http://127.0.0.1:8000`（未自动打开就手动访问该地址）。

> 纯 Python 标准库实现，**无需安装任何第三方包**（需 Python 3.7+）。

## 2. 功能对照

| 终端版功能 | 网页版位置 |
| --- | --- |
| 开始刷题（全题/错题/按题型/混合） | 仪表盘科目卡片「开始刷题」/「复习错题」、顶部「开始刷题」 |
| 继续答题 | 首页「继续答题」卡片 / 「开始刷题」页顶部 |
| 错题本（复习、删除、清空） | 顶部「错题本」 |
| 查询/编辑/删除题目 | 顶部「题库管理」（搜索、按题型过滤、查看详情、编辑、删除） |
| 导入题目 | 顶部「批量导入」（选年级→科目→选 judge→粘贴原文→预览→直接入库）；「题库管理」→「＋ 录入题目」仍可逐题录入 |
| 终端版批量导入 | 终端主菜单「批量导入题目（judge 转换）」：文本先存到 `convert_tools/text.txt`，选 judge 后输出识别数并写 `text_converted.txt`（不自动入库） |
| 生成刷题报告 | 每组刷完**自动保存并弹出**；历史报告在顶部「刷题报告」查看，可删除 |
| 退出自动生成报告 | 网页版每组完成时自动保存，无需手动操作 |

规则与终端版完全一致，例如：

- 普通刷题答错 → 自动记入错题本；错题复习答对 → 自动移出错题本；错题复习仍答错 → 更新时间戳。
- 中途「退出并保存进度」写入的是同一个 `data/brush_progress.json`，因此**终端版也能接着刷网页版保存的进度**（反之亦然）。
- 删除题目会同步清理该题在错题本中的记录。

## 3. 界面使用要点

- **图片题**：题干里出现 `1.png`、`有序表.png` 等文件名时，会自动从 `pictures/` 目录加载图片显示。
- **填空题**：文本中单独一行的 `______` 会显示为填空条；答案按题型对应输入。
- **判断题**：点「✓ 正确 / ✗ 错误」两个按钮即可，答案按 `正确/错误` 存储（与终端版一致）。
- **多选题**：点选所有正确选项后提交（至少选 2 个）。
- **题目查重**（题库管理→侧栏「题目查重」）：同一题型内两两比对（判断/单选/多选/填空各自比，跨题型不比）；选择题会同时比对选项，选项换顺序不影响查重；查重结果可勾选批量删除（删除进回收站）。
- **刷新/关闭页面**：正在进行的练习会话会丢失，但点过「退出并保存进度」后可用「继续答题」找回。

## 4. 常见问题

- **端口被占用**：报 `OSError` 时换端口，如 `python webui_server.py --port 9000`。
- **不想自动开浏览器**：`python webui_server.py --no-browser`，然后手动访问地址。
- **数据不见了**：请确认 `data/`、`pictures/` 与 `webui_server.py` 在同一目录下（启动目录无关紧要，程序按自身文件位置定位）。

## 5. 将来打包成 exe 的提示（供参考）

网页版只依赖标准库，打包很简单：

- 用 PyInstaller：`pyinstaller -F -w webui_server.py`
- 但需要把 `webui/`、`data/`、`pictures/` 目录打进包或放在 exe 旁：
  - 建议：`pyinstaller -F -w --add-data "webui;webui" --add-data "data;data" --add-data "pictures;pictures" webui_server.py`
  - `webui_server.py` 里所有路径都基于 `__file__` 计算；打包成单文件 exe 时 `__file__` 指向临时解压目录，
    因此 `data`/`pictures` 建议放到 exe 同目录并用 `sys._MEIPASS` 兼容（届时按需调整）。
- 若只想打包终端版：`pyinstaller -F launcher.py` 即可（终端版同样会被保留在 exe 里，运行时二选一）。
