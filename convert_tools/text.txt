（               ）字符是一个出现在字符串文本中，以反斜杠（\）开始的特殊字符。
【第1空(1分)】

转义
在Python中，56/5的结果为【第1空(1分)】

11.2
比较字符串大小，其中‘Hello‘比‘world‘【第1空(1分)】。
注：填一个字，大或小

小
若`a=2,b=3`,那么`a**b`的值为【第1空(1分)】。

8
Python 表达式 `10 + 5//3 –True+False`的值为
【第1空(1分)】
.

10
下面语句的输出结果是什么？
【第1空(1分)】
```
print("hello"  'world')     #hello与world间隔两个空格
```

helloworld
逻辑运算符__________
【第1空(1分)】
可以对布尔表达式为真的值进行取反。

not
执行下列语句后的显示结果是什么?
【第1空(1分)】
```
sum = 0
item = 0
while item < 5:
    item += 1
    sum += item
    if sum > 4: break

print(sum)
```

6
执行循环：
```
for i in range(1,5):
    pass
```
后,变量 i 的值是：
【第1空(1分)】

4
有以下程序
```
x = int(input('请输入第一个数：'))
y = int(input('请输入第二个数：'))
if x > y:
    x,y = y,x
print(x,y)
```
程序运行时输入10〈回车〉，12〈回车〉，执行后输出结果是
【第1空(1分)】

10 12
`'abc'*2
的`结果是：
【第1空(1分)】

abcabc
Python中的序列主要有三种：
【第1空(1分)】
、列表和元组。

字符串
`sum((i**2 for i in range(10) if i%2))`的值是
【第1空(1分)】
。

165
`'Zhejiang University'[:-9:-2]`
的结果是：
【第1空(1分)】

'yirv'
下面程序的输出是
【第1空(1分)】
。
```
t=1
t,a=2,t+1
print(a)
```

2
已知`s="Python"`，那么s[2:-1]的值是
【第1空(1分)】
。

tho
阅读下列Python语言程序，写出程序运行结果。
```
sa = [str(i) for i in range(3)]
print(sa)。
```
【第1空(1分)】
。

['0', '1', '2']
`s = "python"，s.replace("n","n程序")`执行结果为
【第1空(1分)】

python程序
根据分支的数量，分支结构可分为单分支、
【第1空(1分)】
和
【第2空(1分)】
结构。

二分支 多分支
以下程序的输出结果是
【第1空(2分)】
```
Is = []
def func(a,b):
    Is.append(b)
    return a * b
s = func("love",2)
print(s,Is)
```

lovelove [2]
已知`x=[1,11,111]`,那么执行语句`x.sort(key=lambda x:len(str(x)),reverse=True)`之后，x的值为。
【第1空(2分)】

[111,11,1]
6-12.下面程序的输出是
【第1空(2分)】
。
```
a=20
def func():
    a=40
    print(a,end=" ")

func()
print(a)
```

40 20
已知`s1="Windows10"，s2="Python3.7"`，则执行`result=''.join(filter(lambda x:x.isdigit(), s1+s2))`之后，result的值为
【第1空(2分)】
。

1037
已知`lista=list(map(str,range(20)))`，则`max(lista)`的值是
【第1空(1分)】
，`max(lista,key=len)`的值是
【第2空(1分)】
。

9 10
已知`lista=[1,2,3,4]`，则`sum(map(lambda x,y:x+y,lista,reversed(lista)))`的值是
【第1空(2分)】
。

20
使用hex,oct,bin函数进行进制转换（高教社，《Python编程基础及应用》习题8-5）
请写出下述程序的执行结果：
```
v =254
print(hex(v))
print(bin(v+1))
print(oct(v+1))
```
执行结果第1行：
【第1空(0分)】
执行结果第2行：
【第2空(1分)】
执行结果第3行：
【第3空(1分)】

0xfe 0b11111111 0o377
下面程序的输出是什么?(字符串不要加引号）
【第1空(2分)】
。
```
lst=[(1,"one"),(2,"two"),(3,"three"),(4,"four")]
lst.sort(key=lambda x:x[1])
print(lst[3][1][2])
```

o
运行代码：`“>>> ord(x) “` 当变量x的值为
【第1空(2分)】
时得到的运行结果为”65”。

A
设有`a=(1,2,3)`; `print(len(a))`运行结果是
【第1空(2分)】

3
下面程序输出结果是什么？
【第1空(2分)】
```
class account:
    def __init__(self,id,balance):
        self.id=id
        self.balance=balance

    def deposit(self,amount):
        self.balance+=amount

    def withdraw(self,amount):
        self.balance-=amount

acc1=account('1234',100)
acc1.deposit(500)
acc1.withdraw(200)
print(acc1.balance)
```

400
类的定义与使用（高教社，《Python编程基础及应用》习题9-2）
请写出下述程序的执行结果：
```
from math import *
class
Circle:
def __init__(self, radius =1):
    self.radius = radius
def getPerimeter(self):
    return 2 * self.radius * pi
def getArea(self):
    return self.radius *self.radius * pi
def setRadius(self, radius):
    self.radius = radius
a = Circle(10)
print
("{:.1f},{:.2f}".format(a.getPerimeter(),a.getArea()))
```
执行结果为：
【第1空(2分)】

62.8,314.16
在Python中，可以使用【第1空(2分)】关键字来声明一个类。

class
Python提供了名称为【第1空(2分)】的构造方法，实现让类的对象完成初始化。

__init__
要正常运行以下程序，请将代码补充完整。
```
class Person(object):
def __init__(【第1空(2分)】,name,gender,birth):

   self.name = name

   self.gender = gender

   self.birth = birth
xiaoming = Person('Xiao Ming', 'Male', '1990-1-1')
print(xiaoming.name)
```

self
Python语句序列：
```
x='123'
print(isinstance(x,int))
```
的运行结果为：
【第1空(2分)】
。

False
打开文件对文件进行读写，操作完成后应该调用
【第1空(2分)】
方法关闭文件，以释放资源。

close
用readlines方法把整个文件中的内容进行一次性读取，返回的是一个
【第1空(2分)】
。

列表
在读写文件的过程中，
【第1空(2分)】
方法可以获取当前的读写位置。

tell
在
【第1空(2分)】
模式下,解释器读取包含Python语句的文件内容并执行每条语句。

脚本
每个Python文件都可以作为一个模块，模块的名字就是
【第1空(2分)】
的名字。

文件
有一个文本文件1.txt，内容如下：

```
1
22
333
4444
55555
```
运行如下代码：
```
f = open('1.txt', 'rt')
k = list(f)
print(len(k))
```
输出结果是：
【第1空(2分)】

5
当使用序列中不存在的
【第1空(2分)】
时，会引发IndexError异常。

索引
一个try语句只能对应一个
【第1空(2分)】
子句。

finally
运行如下程序，输出结果是
【第1空(2分)】
。
```
try:
    x, y = 1, 0
    z = x / y
    print(0, end="")
except:
    print("1", end="")
else:
    print("2", end="")
finally:
    print("3", end="")
```

13
（               ）字符是一个出现在字符串文本中，以反斜杠（\）开始的特殊字符。
【第1空(2分)】

转义
在Python中，56/5的结果为
【第1空(2分)】

11.2
比较字符串大小，其中‘Hello‘比‘world‘
【第1空(2分)】
。
注：填一个字，大或小

小
若a=2,b=3,那么a**b的值为
【第1空(2分)】
。

8
Python 表达式 10 + 5//3 –True+False的值为
【第1空(2分)】
.

10
下面语句的输出结果是什么？
【第1空(2分)】
print("hello"  'world')     #hello与world间隔两个空格

helloworld
逻辑运算符__________
【第1空(2分)】
可以对布尔表达式为真的值进行取反。

not
执行下列语句后的显示结果是什么?
【第1空(2分)】
sum = 0
item = 0
while item < 5:
    item += 1
    sum += item
    if sum > 4: break

print(sum)

6
执行循环：
for i in range(1,5):
    pass
后,变量 i 的值是：
【第1空(2分)】

4
有以下程序
x = int(input('请输入第一个数：'))
y = int(input('请输入第二个数：'))
if x > y:
    x,y = y,x
print(x,y)
程序运行时输入10〈回车〉，12〈回车〉，执行后输出结果是
【第1空(2分)】

10 12
根据分支的数量，分支结构可分为单分支、
【第1空(1分)】
和
【第2空(1分)】
结构。

二分支 多分支
'abc'*2
的结果是：
【第1空(2分)】

abcabc
Python中的序列主要有三种：
【第1空(2分)】
、列表和元组。

字符串
sum((i**2 for i in range(10) if i%2))的值是
【第1空(2分)】
。

165
'Zhejiang University'[:-9:-2]
的结果是：
【第1空(2分)】

yirv
下面程序的输出是
【第1空(2分)】
。
t=1
t,a=2,t+1
print(a)

2
已知s="Python"，那么s[2:-1]的值是
【第1空(2分)】
。

tho
阅读下列Python语言程序，写出程序运行结果。
sa = [str(i) for i in range(3)]
print(sa)。
【第1空(2分)】
。

['0', '1', '2']
s = "python"，s.replace("n","n程序")执行结果为
【第1空(2分)】

python程序
6-12.下面程序的输出是
【第1空(2分)】
。
a=20
def func():
    a=40
    print(a,end=" ")

func()
print(a)

40 20
6-13.下面程序的运行结果是（如程序错误写"error"）
【第1空(2分)】
。
b,c=3,4
def g_func():
    a=b*c
    b=b+1
    d=a
    print(a,b,end=" ")
g_func()
print(b,c)

error
6-14.下面程序的运行结果是（如程序错误写“error”）
【第1空(2分)】
。
b,c=3,4
def g_func(d):
    global a
    a=d*c
g_func(b)
print(a)

12
已知函数定义 def funct(x,y,z=20):return x+y+z, 那么表达式func(10,15)的值为
【第1空(2分)】

45
已知函数定义 def func(x,y,z):return x+y-z, 那么表达式func(y=2,z=3,x=8)的值为_____
【第1空(2分)】

7
执行下列语句后的显示结果是什么?
【第1空(2分)】
def m(list):
    v = list[0]
    for e in list:
      if v < e: v = e
    return v

values = [[3, 4, 5, 1], [33, 6, 1, 2]]
for row in values:
    print(m(row), end = " ") #一个空格

5 33
以下程序的输出结果是
【第1空(2分)】
Is = []
def
func
(
a,b
):
Is.append(b)
return
a * b
s = func(
"love"
,
2
)
print
(s,Is)

lovelove [2]
已知x=[1,11,111],那么执行语句x.sort(key=lambda x:len(str(x)),reverse=True)之后，x的值为。
【第1空(2分)】

[111,11,1]
表达式 map(lambda x:x+5,[1,2,3,4,5])的值为____。
【第1空(2分)】

[6,7,8,9,10]
表达式sum(list(range(1, 10, 3)))的值是（）
【第1空(2分)】
。

12
已知s1="Windows10"，s2="Python3.7"，则执行result=''.join(filter(lambda x:x.isdigit(), s1+s2))之后，result的值为
【第1空(2分)】
。

1037
已知lista=list(map(str,range(20)))，则max(lista)的值是
【第1空(1分)】
，max(lista,key=len)的值是
【第2空(1分)】
。

9 10
已知lista=[1,2,3,4]，则sum(map(lambda x,y:x+y,lista,reversed(lista)))的值是
【第1空(2分)】
。

20
使用hex,oct,bin函数进行进制转换（高教社，《Python编程基础及应用》习题8-5）
请写出下述程序的执行结果：
v =
254
print
(
hex
(v))
print
(
bin
(v+
1
))
print
(
oct
(v+
1
))
执行结果第1行：
【第1空(0分)】
执行结果第2行：
【第2空(1分)】
执行结果第3行：
【第3空(1分)】
拼尽全力还是不会？参考B站习题讲解
哔哩哔哩up主：
海洋饼干叔叔
Python课程
Python习题
简洁的C和C++
作者每天分享一篇关于C/C++/Python的技术文章，学习编程不迷路。

0xfe 0b11111111 0o377
下面程序的输出是什么?(字符串不要加引号）
【第1空(2分)】
。
lst=[(1,"one"),(2,"two"),(3,"three"),(4,"four")]
lst.sort(key=lambda x:x[1])
print(lst[3][1][2])

o
运行代码：“>>> ord(x) “ 当变量x的值为
【第1空(2分)】
时得到的运行结果为”65”。

A
设有a=(1,2,3); print(len(a))运行结果是
【第1空(2分)】

3
下面程序输出结果是什么？
【第1空(2分)】
class account:
    def __init__(self,id,balance):
        self.id=id
        self.balance=balance

    def deposit(self,amount):
        self.balance+=amount

    def withdraw(self,amount):
        self.balance-=amount

acc1=account('1234',100)
acc1.deposit(500)
acc1.withdraw(200)
print(acc1.balance)

400
类的定义与使用（高教社，《Python编程基础及应用》习题9-2）
请写出下述程序的执行结果：
from
math
import
*
class
Circle
:
def
__init__
(
self, radius =
1
):
self
.radius = radius
def
getPerimeter
(
self
):
return
2
*
self
.radius * pi
def
getArea
(
self
):
return
self
.radius *
self
.radius * pi
def
setRadius
(
self, radius
):
self
.radius = radius
a = Circle(
10
)
print
(
"{:.1f},{:.2f}"
.
format
(a.getPerimeter(),a.getArea()))
执行结果为：
【第1空(2分)】
拼尽全力还是不会？参考B站习题讲解
哔哩哔哩up主：
海洋饼干叔叔
Python课程
Python习题
简洁的C和C++
作者每天分享一篇关于C/C++/Python的技术文章，学习编程不迷路。

62.8,314.16
在Python中，可以使用
【第1空(2分)】
关键字来声明一个类。

class
Python提供了名称为
【第1空(2分)】
的构造方法，实现让类的对象完成初始化。

__init__
要正常运行以下程序，请将代码补充完整。
class Person(object):
def __init__(
【第1空(2分)】
,name,gender,birth):

   self.name = name

   self.gender = gender

   self.birth = birth
xiaoming = Person('Xiao Ming', 'Male', '1990-1-1')
print(xiaoming.name)

self
Python语句序列：
x='123'
print(isinstance(x,int))
的运行结果为：
【第1空(2分)】
。

False
打开文件对文件进行读写，操作完成后应该调用
【第1空(2分)】
方法关闭文件，以释放资源。

close
用readlines方法把整个文件中的内容进行一次性读取，返回的是一个
【第1空(2分)】
。

list
在读写文件的过程中，
【第1空(2分)】
方法可以获取当前的读写位置。

tell
在
【第1空(2分)】
模式下,解释器读取包含Python语句的文件内容并执行每条语句。

脚本
每个Python文件都可以作为一个模块，模块的名字就是
【第1空(2分)】
的名字。

文件
有一个文本文件
1.txt
，内容如下：
1
22
333
4444
55555
运行如下代码：
f = open('1.txt', 'rt')
k = list(f)
print(len(k))
输出结果是：
【第1空(2分)】

5
当使用序列中不存在的
【第1空(2分)】
时，会引发IndexError异常。

index
一个try语句只能对应一个
【第1空(2分)】
子句。

finally
运行如下程序，输出结果是
【第1空(2分)】
。
try:
    x, y = 1, 0
    z = x / y
    print(0, end="")
except:
    print("1", end="")
else:
    print("2", end="")
finally:
    print("3", end="")

13
